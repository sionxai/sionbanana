# macOS
.DS_Store

# local tooling
.claude/

# dependencies
/node_modules

# build output
/.next
/out
/dist
/build

# env files
.env
.env.*
.env.local
!.env.example

# local data (이미지, SQLite)
/data/

# 이미지/스토리보드 생성 세션 부산물 (data/storyboard/에 두는 게 정석, 루트 유출 안전망)
*.run.log
*.summary.json

# misc
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.vscode
.idea

# TypeScript incremental build cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# Firebase debug logs (legacy SaaS, 안전망)
firebase-debug.log
